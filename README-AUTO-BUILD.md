## Compilation automatique

Le workflow GitHub Actions `.github/workflows/build-installer.yml` compile automatiquement PC Optimizer sur Windows et produit `PCOptimizer-Setup-v6.1.exe` comme artefact.

Pour créer une Release téléchargeable, créer ensuite un tag `v6.1.0` sur la branche `main`.
